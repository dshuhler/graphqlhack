type Book {
    title: String!
    author: String!
    genre: [String]
}

type Query { # this is a special type
    findAllBooks: [Book]!
}

type Mutation {
    newBook(title: String!, author: String!) : Book!
}